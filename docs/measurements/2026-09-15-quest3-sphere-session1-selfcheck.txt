# Самопроверка шар-меню, Quest 3, 2026-09-15, запуск 19:51:37 (сессия 1, шаг 1б, 9 проверок).
# Источник: вывод команды сбора сразу после самопроверки (logcat тег godot). Файл при первом
# копировании вышел пустым — захват logcat мастера (wizard_logcat.txt) не записался, а grep шёл по
# нему; строки восстановлены из вывода той же сессии без правок.
# Замечание: у линзы «промахов по бюджету нет» при 26% доставки — счётчик промахов окна не видит
# кадров, которых не было; вывод по доставке, не по счётчику (открыто).
частота: запрошено 90, получено 90.0 (ok)
  PASS  XR: интерфейс поднят, вьюпорт в шлем
  PASS  частота 90.0 Гц — цель ADR-0007
  PASS  MSAA 2x на XR-вьюпорте (ADR-0003 п. 6)
  PASS  прогрев: за 30 кадров первого открытия (ячейки, панель, луч) surface 0 { "surface": 0, "draw": 1, "specialization": 1 }; пик CPU+скрипты 4.7 мс
  PASS  глобус m=8, 642 ячеек, вращение: p95 GPU 2.76 / CPU рендера+скриптов 5.57 мс из 11.11, связывает CPU (рендер 0.24 + скрипты 5.33); доставлено 135 кадров за 1501 мс = 89.9 к/с из 90 (100%); промахов по бюджету нет
  FAIL  линза α=0.044, 700 ячеек, вращение: p95 GPU 3.34 / CPU рендера+скриптов 45.92 мс из 11.11, связывает CPU (рендер 0.16 + скрипты 45.76); доставлено 35 кадров за 1506 мс = 23.2 к/с из 90 (26%); промахов по бюджету нет
  PASS  атлас: 1024x1024, перерисован 1 раз, иконки загружаются
  PASS  панель: перерисована 19 раз, предпросмотр изображения 256x256
  PASS  пропуск перерисовки: неподвижный глобус пересчитан 0 раз, скрипты p95 2.49 мс против 5.33 при вращении
passed=8 failed=1 unknown=0
пол: исполнено 9/9 проверок
