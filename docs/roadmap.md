# Дорожная карта

Этапы, критерии готовности и то, чего в этапе намеренно нет. Принцип — одна нить за раз
(PRACTICES §7.5). Ресурс: соло, длинный горизонт, без спешки.

---

## Этап 0 — фундамент, контекст и прибор  ◀ текущий

**Смысл:** не начать писать движок, а сделать так, чтобы следующие этапы опирались на измеренные
числа и проверенные имена API, а не на текст исследования.

| Шаг | Статус |
|---|---|
| 0.1 `git init`, `.gitignore`, файлы контекста (`CLAUDE.md`, `docs/`) | **готово** |
| 0.2 `godot/` submodule на `4.7.2-stable` | **готово** — пин `ed1daf0`, сверен с `version.py` |
| 0.3 Android SDK + NDK, сборка шаблона, приложение на шлеме | **готово** — SDK 2.9 ГБ, NDK 29.0.14206865, APK развёрнут и запущен на Quest 3 |
| 0.4 Прибор `modules/vrge_probe/` — паспорт железа | **готово**: расширения, вердикты движка, лимиты, стоимость draw call, разогрев PSO, троттлинг, запрос частоты, свойства системы OpenXR, частотная матрица, клок GPU с хоста, промахи по дедлайну, ручки рендера и поверхности меню (фаза L), лестница под MSAA (L4). Размер тайла отложен — нужен патч в `godot/` |
| 0.5 ADR-0003 «рендер-путь» | **принят частично** (2026-09-13, прогоны 10–11): Forward Mobile + multiview, батчинг ради GPU, MSAA 2x, без VRS_XR, шар в мировом буфере. Открыто: форма кривой «время от числа вызовов», frame synthesis, прекомпиляция шейдеров |

**Готово когда** (статус на 2026-09-12):

1. ✅ `tools/build_quest.sh` собирает; `verify_artifact.sh` — 4/4, включая проверку Swappy.
2. ✅ APK ставится на Quest 3 и запускается; лог подтверждает Vulkan 1.3.295 и Forward Mobile.
3. ✅ Прибор отработал, пол сошёлся: 19/19.
4. ✅ Контрольные случаи `VK_KHR_swapchain` и `XR_KHR_vulkan_enable2` — PASS.
5. ✅ Фальсификаторы vk и xr — точечные, различают.
6. ✅ `docs/hardware-profile.md` заполнен числами: мкс/draw call по CPU и GPU, цена разогрева
   PSO, статистика 1080 выборок за 18 минут, вердикт по троттлингу.
7. ✅ Раздел «что не измерено» есть в паспорте.

**Этап 0 закрыт по числам.** Остались намеренно отложенные вещи, каждая с причиной: размер тайла
(патч в `godot/`, решение владельца), экономия от MSAA (нужны GPU-счётчики), поведение на 90 Гц,
троттлинг за пределами 18 минут.

**Долг по прибору после аудита 2026-09-12 — закрыт.** Аудит нашёл, что вывод о троттлинге
опирался на слежение за сменой частоты, тогда как прогон шёл на 72 Гц. Добавленные в прибор
опрос частот и проверка дрейфа прогнаны на устройстве (4-й прогон, 17:47):

- частоты измерены: **[72, 80, 90, 120]** — 72 действительно минимальна, гипотеза аудита верна;
- дрейф проверяется прибором, вердикт «устойчивой деградации нет» больше не опирается на ручной
  разбор TSV.

Остаточный долг, названный явно: метрика дрейфа **ни разу не краснела**, то есть её различающая
способность не подтверждена, а порог 5% назначен по двум прогонам при фактическом +4.3% — запас
невелик.

**Целевая частота выбрана и измерена (ADR-0007, прогон 9).** Цель — 90 Гц (11.11 мс), минимум —
72 Гц (13.89 мс). На цели 1600 объектов занимают 8.93–9.51 мс **без промахов по дедлайну**, цена
draw call 4.037 мкс, цена пикселя ≈117–140 мкс на мегапиксель.

**В рабочем диапазоне загадки нет.** Разницу 72↔90 объясняет разгон GPU: остаток после нормировки
на клок 1.047. Необъяснённые ≈1.5 остались только у пары 72↔120 и **перестали быть блокирующими**:
цена пикселя от частоты не зависит, значит эффект в подаче геометрии, а не в затенении. Прямая
проверка версии про режим GMEM упирается в `VK_QCOM_tile_properties`, которого Godot не включает.

**Запас на 120 Гц исчерпан** — при 1600 объектах 32–40% кадров мимо дедлайна, — и потому 120 Гц
целью не являются.

**Потолок выше заявленного и объяснён.** 144 Гц работают вопреки перечислению устройства; по
документации Meta Quest 3 с HorizonOS ≥ 2.7 даёт **72–207 Гц без настройки**, а список частот
неисчерпывающ по замыслу (`docs/research/sources.md`, ADR-0006 с поправкой). Значит рабочая
частота выбрана не потолком, а неполным перечислением. Какую частоту делать целью — решение
владельца и новый ADR; при 1600 объектах уже на 120 Гц треть кадров мимо дедлайна.

Не прогонялись: троттлинг на 120 Гц (фаза E), 80 и 90 Гц.

**ADR-0003 «рендер-путь» записан** (прогоны 10–11). MSAA 2x не дороже выключенного ни в одной
точке и дешевле на 200–800 вызовах при равном клоке. Выяснилось, что время кадра от числа вызовов
**нелинейно**: линейная формула бюджета занижает середину, бюджет пока берётся по измеренной точке.

**Следующий шаг — на выбор владельца:** плотная лестница вызовов с клоком по точкам (форма кривой
для бюджета) либо переход к этапу 1 — стоимость кандидатов шара уже измерена и бюджет не
ограничивает.

**Чего в этапе нет:** шар-меню, собственный RHI/ECS/Jolt/Yoga/MSDF, формат сцен, экспорт проекта,
PCVR и Linux-сборка.

---

## Этап 1 — шар-меню в VR

**Смысл:** проверить самую рискованную гипотезу проекта — что сигнатурный UX вообще удобен. Если шар
неудобен, знать об этом надо до того, как под него написан движок.

Гексагональная сетка на сфере в руках пользователя; поворот стиком или рукой перебирает пункты;
прокрутка элементов происходит на **дальней от камеры** стороне шара; нажатие выбирает пункт.

**Открытые вопросы этапа:** гексагональная сетка на сфере не бывает регулярной (нужны 12 пятиугольных
дефектов — как у футбольного мяча); как это скрыть или обыграть в UX — не решено.

**Вариант «слой на ячейку» закрыт измерением:** лимит composition layers на Quest 3 — **32**, а
этап требует поиска пункта из 40+. Остаются один слой с нарисованной текстурой либо сетка в
мировом буфере; выбор между ними — предмет ADR-0003.

**Готово когда:** прототип на шлеме, человек вслепую находит нужный пункт из 40+, кадровый бюджет
подтверждён прибором, решение по дефектам сетки записано в ADR.

---

## Дальше (порядок не зафиксирован)

- Формат проекта и экспорт как отдельного приложения — **требует ADR до первой строки кода**.
  Кодогенерация с APK-упаковкой на устройстве или рантайм-интерпретация данных — архитектуры разные.
- Расстановка объектов в VR: взять рукой, поставить, сохранить, загрузить.
- Редактор логики событий; редактор сюжета.
- Процедурная генерация; голосовой ввод.
- PCVR-ветка (Linux/Windows), стриминг.

Ничто из этого не планируется детально, пока не закрыт этап 1.

## Когда обновлять этот файл

- Этап сменил статус или закрыт.
- Появился блокер, меняющий порядок этапов.
- Владелец изменил приоритет вехи.
- Открытый вопрос этапа получил ответ — перенести в ADR и убрать отсюда.
